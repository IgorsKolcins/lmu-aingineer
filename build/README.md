Place a Windows icon at `build/icon.ico` before running `bun run dist:win:portable` for the Windows x64 portable build.

The Windows portable build uses that file for the packaged app metadata and
executable icon.
